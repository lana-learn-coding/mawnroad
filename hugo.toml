baseURL = 'https://lana-learn-coding.github.io/mawnroad/'
languageCode = 'en-us'
title = 'Mawnroad'
theme = 'mainroad'
themesDir = "../themes"

[params.author]
name = "Manwgo"
bio = "Manwgo's true identity is unknown. Maybe he is a successful blogger or writer. Nobody knows it."

[params.style.vars]
highlightColor = "#a16207" # Override highlight color

[params.logo]
subtitle = "Playground for testing various stuff"

[params.sidebar]
home = "right" # Configure layout for home page
list = "left"  # Configure layout for list pages
single = false # Configure layout for single pages
widgets = ["search", "recent", "categories", "taglist", "social", "languages"]